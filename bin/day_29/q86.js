function checkWord(sentence) {
    return sentence.includes("javascript"); //check the word javascript if find the javascript so it's return true otherwise return false.
}
;
//Example check function perform correctly or not.
console.log(checkWord("I like to coding in javascript"));
export {};
