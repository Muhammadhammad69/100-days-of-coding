function extractingCharactersFromString(sentence) {
    return sentence.substring(0, 10); //extract first ten characters of strings.
}
;
//example for extracting ten characters of string.
console.log(extractingCharactersFromString("I like the Typescript language."));
export {};
