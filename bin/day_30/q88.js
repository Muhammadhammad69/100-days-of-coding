function convertStringIntoNumber(str) {
    return Number(str); // return the number type.
}
;
//example 
console.log(convertStringIntoNumber("456")); //convert the type string into numbers.
export {};
